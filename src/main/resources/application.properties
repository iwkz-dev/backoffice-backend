## Server Properties
server.port=9001

## App Properties
app.jwtSecret=Al-Falah,1WKZ!!KeyReplaceThisOnProd
app.jwtExpirationInMs=604800000

## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url= jdbc:mysql://remotemysql.com:3306/DdpnJEwdDC?useSSL=true&serverTimezone=UTC&useLegacyDatetimeCode=false
spring.datasource.username=DdpnJEwdDC
spring.datasource.password=ilrlDN98PQ


## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.hibernate.ddl-auto=create-drop

## Hibernate Logging
logging.level.org.hibernate.SQL=DEBUG

# Initialize the datasource with available DDL and DML scripts
spring.datasource.initialization-mode=always

## Jackson Properties
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS=false
spring.jackson.time-zone=UTC

## Spring Profiles
# spring.profiles.active=prod